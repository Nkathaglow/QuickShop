# 🛍️ QuickShop

A modern, responsive mini e-commerce application built with React. This project demonstrates fetching data from an external API, managing component state, and creating a clean grid UI.

## 🚀 Features
* **Dynamic Product Feed:** Fetches real-time product data from the Fake Store API.
* **Responsive Grid:** Adapts seamlessly to different screen sizes.
* **Loading States:** Handles asynchronous data fetching gracefully.
* **Error Handling:** Fails safely if the API is down.

## 🛠️ Tech Stack
* **Frontend:** React (Hooks), Vite
* **Styling:** CSS3 (Flexbox & Grid)
* **API:** Fake Store API

## 📦 How to Run
Follow these steps to run the project locally:

1.  **Clone the repository**
    ```bash
    git clone [https://github.com/YOUR_USERNAME/quickshop.git](https://github.com/YOUR_USERNAME/quickshop.git)
    ```

2.  **Navigate to the folder**
    ```bash
    cd quickshop
    ```

3.  **Install dependencies**
    ```bash
    npm install
    ```

4.  **Start the server**
    ```bash
    npm run dev
    ```

## 🔮 Future Improvements (Roadmap)
 **Product Details:** Routing to individual product pages.
 **Shopping Cart:** Global state management (Context API) to add/remove items.
 **Category Filters:** Filter products by category (Electronics, Jewelery, etc).

---
*Created as part of a React Code Challenge.*
